# MVP Validation required-check smoke test

This temporary documentation-only change exists solely to verify that the
`MVP Validation` repository ruleset blocks a pull request while the aggregate
check is pending and permits merging only after the check succeeds.

The pull request will be closed without merging after verification.
